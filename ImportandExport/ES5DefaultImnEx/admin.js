//const{u_id}=require('./user')

const nani=require('./user') // In default Import and export we can change the name of the what we exported 
                             //it is called Alias

console.log(nani)
